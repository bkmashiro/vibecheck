{
  "name": "vibecheck",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "worker"
  ],
  "scripts": {
    "build": "pnpm --filter vibecheck-frontend build && pnpm --filter vibecheck-worker typecheck",
    "test": "pnpm --filter vibecheck-worker test",
    "typecheck": "pnpm -r typecheck",
    "dev:worker": "cd worker && wrangler dev",
    "dev:frontend": "cd frontend && vite",
    "deploy:worker": "cd worker && wrangler deploy",
    "deploy:frontend": "cd frontend && vite build && wrangler pages deploy dist --project-name vibecheck"
  },
  "packageManager": "pnpm@10.30.3"
}
